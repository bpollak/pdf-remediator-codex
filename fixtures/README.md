# PDF Fixtures

The project expects these fixture names for manual and automated QA:

- `accessible.pdf`
- `untagged.pdf`
- `partial.pdf`

These fixture files should be added in future iterations. The current tests use in-memory parsed PDF mocks for deterministic unit checks.
